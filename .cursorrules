# 工位管理系统 - 开发规则

## 开发流程

### 本地开发（测试阶段）

```bash
cd "/Users/L/工作（YJ）/工位管理"

# 启动本地开发服务器
npm run dev
```

- **电脑访问**: http://localhost:5174/
- **手机访问**: http://192.168.1.16:5174/ （同一 WiFi）
- 修改代码后自动热更新，无需重启

### 推送更新（满意后）

```bash
# 1. 确认本地效果满意

# 2. 构建生产版本
npm run build

# 3. 提交到 GitHub
git add .
git commit -m "描述修改内容"
git push

# ✅ Cloudflare Pages 自动部署（1-2分钟）
```

## 重要提示

⚠️ **先本地测试，满意后再推送**
- 不要立即 `git push`
- 本地 `npm run dev` 测试效果
- 确认无误后再构建和推送

🔄 **自动部署机制**
- 推送到 GitHub 后自动部署
- 无需手动操作 dist 文件
- 部署状态可在 Cloudflare Pages 查看查看

📱 **手机测试**
- 确保手机和电脑在同一 WiFi
- 使用 Network 地址（如 192.168.1.16:5174）
- 横屏浏览体验更佳
